# TOPIK 韓単語マスター - 技術仕様・音韻ルール・システム詳細ドキュメント

本ドキュメントは、**TOPIK 韓単語マスター (Flashcard & Quiz Web App)** で採用されている韓国語音韻・ローマ字変換ルール、音声合成・効果音処理、データ抽出・クイズ生成アルゴリズム、およびモバイルUI最適化の技術仕様をまとめたローカル参照用ドキュメントです。

---

## 1. 🇰🇷 韓国語ローマ字変換（Romanization）と音韻処理

### 1.1 採用規格
本アプリのローマ字表記は、韓国文化観光部が定めた公式規格 **「韓国語のローマ字表記法（Revised Romanization of Korean: RR / 2000年告示）」** に準拠しています。

### 1.2 子音の文頭・文中・終声（パッチム）の扱いルール

平音（`ㄱ, ㄷ, ㅂ, ㄹ`）は、音節内での位置や後続する母音の有無によってアルファベット表記が切り替わります：

| 韓国語子音 | 母音の前（文頭・文中） | 終声（パッチム）/ 無声化時 | 具体的表記例 |
| :---: | :---: | :---: | :--- |
| **ㄱ (giyeok)** | **`g`** | **`k`** | **가게** (店) ➔ `gage`<br>**국** (スープ) ➔ `guk` |
| **ㄷ (digeut)** | **`d`** | **`t`** | **다** (全部) ➔ `da`<br>**듣다** (聞く) ➔ `deutda` |
| **ㅂ (bieup)** | **`b`** | **`p`** | **바다** (海) ➔ `bada`<br>**밥** (ご飯) ➔ `bap` |
| **ㄹ (rieul)** | 母音の前: **`r`** | パッチム / 重音(ㄹㄹ): **`l`** | **라면** (ラーメン) ➔ `ramyeon`<br>**달** (月) ➔ `dal`<br>**팔리다** (売れる) ➔ `pallida` |

> 💡 **有声化・無声化と表記の慣例**
> 旧表記（マッキューン＝ライシャワー式）では文頭の `ㄱ/ㄷ/ㅂ` を `k/t/p` と書いていましたが、現行の標準表記法（RR式）では**「母音の前であれば文頭・文中を問わず `g/d/b`」**と表記するよう統一されています。

### 1.3 激音と濃音の表記ルール
- **激音（息を強く出す音）**: `ㅋ` = **`k`**, `ㅌ` = **`t`**, `ㅍ` = **`p`**, `ㅊ` = **`ch`**
  - 例: **파티** ➔ `pati`
- **濃音（詰まる音）**: `ㄲ` = **`kk`**, `ㄸ` = **`tt`**, `ㅃ` = **`pp`**, `ㅆ` = **`ss`**, `ㅉ` = **`jj`**
  - 例: **가꾸다** ➔ `gakkuda`

### 1.4 音声合成（TTS）とローマ字表示の役割分担
- **テキスト表記（視覚補助）**: 上記の標準RR表記に沿って、スペルと対応した整然としたアルファベットを表示します。
- **🔊 音声再生（聴覚学習）**: ブラウザの **Web Speech API（`ko-KR` ネイティブ音声合成）** を使用するため、連音化や鼻音化・濃音化など、実際の会話における韓国語の自然な音韻変化（音声）が流れます。

---

## 2. 📊 データ処理・自動変換パイプライン

### 2.1 データ構成
- **初級データ (TOPIK I / 1,671語)**: `topik-1671_romaji.csv`
- **中級データ (TOPIK II / 2,662語)**: `topik-2662_romaji.csv`
- **統合JSデータセット**: `data.js` (`window.TOPIK_DATASETS`)

### 2.2 PDFデータ抽出と `korean-romanizer` による自動変換
中級単語帳（`topik-2662.pdf`）は単語番号・ハングル・日本語意味のみで構成されていたため、以下のPython処理系でローマ字を全件生成しました：

```python
import pypdf
import re
import csv
from korean_romanizer.romanizer import Romanizer

# PDFの2段組構造から正確に (No, Hangul, Japanese) を抽出
# Romanizer を用いて標準RR規則でアルファベット読みを自動生成
romaji = Romanizer(hangul_text).romanize()
```

### 2.3 データクリーニング
- PDFヘッダーの混入パターン（例: `パーセント 1599 学校 学校` ➔ `パーセント`）の完全自動検知とクレンジング。
- 全CSVの改行コードを LF に統一し、環境依存による `\r` の混入を徹底排除。

---

## 3. 🎯 クイズエンジン & 選択肢非重複アルゴリズム

### 3.1 選択肢重複排除（0% Collision Algorithm）
同義語（例: 「店」を意味する複数の単語）が存在する場合に、画面上に同じ選択肢テキストが複数表示される問題を防ぐため、以下のロジックでダミー選択肢（誤答肢）を生成しています：

```javascript
// モードに応じた正解テキストを取得
const targetText = mode === 'jp-to-kr' ? wordItem.hangul.trim() : wordItem.japanese.trim();

// 正解テキストおよび既に選ばれた選択肢テキストと重複しない単語のみを抽出
const pool = allWords.filter(w => {
  if (w.id === wordItem.id) return false;
  if (mode === 'jp-to-kr') {
    return w.hangul.trim() !== wordItem.hangul.trim() && w.japanese.trim() !== wordItem.japanese.trim();
  } else {
    return w.japanese.trim() !== wordItem.japanese.trim() && w.hangul.trim() !== wordItem.hangul.trim();
  }
});

// 4つの選択肢テキストが100%ユニークになるよう厳格選択
```

### 3.2 スコア & コンボ計算式
$$\text{Score}_{\text{add}} = 10 + (\text{Streak} \times 2)$$
- 正解ごとに基本10pt ＋ 連続正解数（🔥コンボ）に応じたボーナスポイントを加算。

---

## 4. 🔊 Web Audio API & 音声合成アーキテクチャ

### 4.1 音声合成 (Web Speech API)
- `SpeechSynthesisUtterance` (`lang: 'ko-KR'`)
- 話速（`rate: 0.88`）に調整し、聞き取りやすいネイティブ発音を提供。
- タブ切り替え・問題移動時に `window.speechSynthesis.cancel()` を呼び出し、音声の重複再生を防止。

### 4.2 Web Audio API による合成効果音
外部の音声ファイル（.mp3/.wav）に依存せず、ブラウザ内部の `AudioContext` オシレーターで効果音をリアルタイム合成：

- **正解チャイム (Correct Sound)**: `sine` 波形 / C5 (523.25Hz) ➔ G5 (783.99Hz) の上昇トーン。
- **不正解エラー音 (Wrong Sound)**: `sawtooth` 波形 / A3 (220Hz) ➔ E3 (164.81Hz) の低音トーン。

---

## 5. 📱 モバイルUI最適化（6インチスマホ対応）

### 5.1 レスポンシブ設計 (`@media (max-width: 640px)`)
- **パディング最適化**: `.glass-panel` の余白を `2.5rem` から `1.0rem` に収縮し、画面有効面積を最大化。
- **フォントサイズ可変調整**: クイズ出題の文字サイズを `3rem` から `2.1rem` に落とし、`word-break: break-word` で改行崩れを防止。
- **タッチターゲット保証**: 選択肢カードの最小高さを `52px` に設定し、誤タップを防止。
- **キーボード防御**: 辞書検索入力フォーム（`<input>`）入力時はクイズの数字キーショートカットを自動無効化。

---

## 6. ⚠️ LocalStorage 誤答永続化 & 弱点特訓システム

### 6.1 完全クライアントサイドのデータストレージ設計
GitHub Pagesのような静的ウェブ環境でユーザー個別の誤答データを維持するため、ブラウザの `localStorage`（Key: `topik_wrong_words_v1`）を採用しています。

- **データ仕様**:
  ```json
  {
    "beginner": [3, 15, 42, 108],
    "intermediate": [12, 89]
  }
  ```
- **自動記録**: クイズ回答時に不正解を選んだ場合、当該単語の `id` を `localStorage` の配列に自動追加。
- **自動克服**: 「弱点克服モード」で出題された問題に正解すると、配列から該当 `id` が自動削除されます。

### 6.2 クイズ・単語帳との連動
- **出題ソース選択 (`state.source = 'wrong'`)**: クイズ開始時、`localStorage` の誤答IDに一致する単語のみを出題プールにフィルタリング。
- **単語カード復習**: 復習帳画面から「単語カードで復習」を押すことで、誤答単語だけをカードめくり形式で集中暗記可能。

---

*本ドキュメントはローカルでの技術検証・仕様記録用として作成されています。*
